$('body').append('<%= j render "board" %>');

<% if @player == "B" %>
  $('.checkers-board')[0].style.transform = "rotate(180deg)";
<% end %>

$('.turn-board').html("Black starts.");
$('.undo-text').html("UNDO");

<% if @mode == "ai" && @player == "W" %>
  var board = $(".checkers-board").data('board');
  $.ajax({
    type: "GET",
    url: "/ai_move",
    data: { board: board, player: player },
    dataType: 'script'
  });
<% end %>
